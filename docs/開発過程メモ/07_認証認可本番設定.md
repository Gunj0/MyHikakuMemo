# 認証認可本番確認

## 暗号鍵生成

```zsh
# 32byte (256bit) のランダムバイトを生成し、Base64エンコード
openssl rand -base64 32
```

## 環境変数を設定

```text
Jwt__Issuer: {バックエンドのURL}
Jwt__Audience: {フロントエンドのURL}
Jwt__SecretKey: {生成した暗号鍵}
```
