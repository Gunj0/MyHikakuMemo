# 暗号鍵生成

## Base64 形式で出力

- 最も一般的, 鍵の長さは約 43 文字

```zsh
# 32byte (256bit) のランダムバイトを生成し、Base64エンコード
openssl rand -base64 32
```

## 16 進数 (Hex) 形式で出力

- 特定のシステムで利用, 鍵の長さは 64 文字

```zsh
# 32byte (256bit) のランダムバイトを生成し、16進数エンコード
openssl rand -hex 32
```
